Vendored fonts for the generated RDoc theme.

Sources:

- Inter Latin and Latin Extended WOFF2 files from Google Fonts.
- Stabil Grotesk Regular, Medium, and Bold WOFF2 files from lavinmq.com.

These files are copied into `doc/fonts/` by `bundle exec rake rdoc` so the
generated documentation does not depend on remote font hosts.
